(* unification error: fac must be used monomorphically in def *)

val operator_if : Bool -> 'aaa -> 'aaa -> 'bbb
let operator_if c t e = _if c _then t _else e

let rec fac n = fac (n - 1) + (fac false)

let main = 1
